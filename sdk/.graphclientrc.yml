sources:
  - name: Hypercerts
    handler:
      graphql:
        # Default value is hypercerts-testnet
        # This is needed for the initial introspection on buildtime
        endpoint: https://api.thegraph.com/subgraphs/name/hypercerts-admin/{context.graphName:hypercerts-testnet}

additionalTypeDefs: |
  extend type Claim {
    graphName: String!
  }
   extend type ClaimToken {
    graphName: String!
  }

additionalResolvers:
  - ./src/utils/resolvers.ts

documents:
  - ./src/indexer/queries/*.graphql

codegen:
  contextType: "MeshContext & { graphName: string }"

plugins:
  - pollingLive:
      defaultInterval: 5000
