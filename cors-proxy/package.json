{
  "name": "@hypercerts-org/cors-proxy",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230115.0",
    "typescript": "^4.9.5",
    "vitest": "^0.28.4",
    "wrangler": "2.9.1"
  },
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 3000",
    "deploy": "wrangler publish",
    "lint": "tsc --noEmit && eslint --ignore-path ../.gitignore --max-warnings 0 --cache --fix . && prettier --ignore-path ../.gitignore --loglevel warn --write .",
    "test": "vitest --run"
  }
}
